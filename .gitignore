# Python
__pycache__/
*.pyc
*.pyo

# Logs
*.log
nohup.out
nohup_*.log
sweep.log

# Data files (large, stored outside the repo)
*.root
*.h5
*.npy

# Model artifacts and HLS-generated projects.
# Ignore everything under models/ EXCEPT the doc that explains what belongs
# there. (Ignore the contents, not the dir itself, so the negation can match.)
models/*
!models/MODEL.md
software/

# Experiment tracking
wandb/

# Generated plot outputs (ROC curves, loss/AUC/kinematics plots, model diagrams).
# Documentation assets under docs/ are kept tracked via the negations below.
*.png
*.pdf
!docs/**/*.png
!docs/**/*.pdf

# Claude Code internal
.claude/

# Accidentally nested clone of this repo (kept on disk, not tracked)
BNJetTagKai/
